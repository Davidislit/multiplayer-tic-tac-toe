version: '3'
services:
  server:
    build: server
    ports:
      - '5000:5000'
    restart: on-failure
    container_name: tictac-server
  client:
    build: client
    ports:
      - '81:80'
    restart: on-failure
    container_name: tictac-client
    links:
      - server